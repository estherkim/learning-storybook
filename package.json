{
  "type": "module",
  "dependencies": {
    "vite": "^3.2.5"
  },
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf dist",
    "storybook": "npm start --prefix packages/storybook"
  },
  "workspaces": [
    "packages/storybook"
  ]
}
