{
  "name": "example-preact",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf dist",
    "storybook": "npm start --prefix packages/storybook"
  },
  "devDependencies": {
    "typescript": "4.9.3"
  },
  "workspaces": [
    "packages/storybook"
  ]
}
